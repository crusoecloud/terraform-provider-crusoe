package manifest

// apiDesc* — schema descriptions derived from the client-go swagger spec (Manifest).
const (
	apiDescDigest = "Content-addressable digest that uniquely identifies the manifest."
	apiDescSize   = "Total size of the manifest, in bytes."
	apiDescTags   = "Tags that point to the manifest."
)
